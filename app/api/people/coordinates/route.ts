import { NextResponse } from "next/server";export async function GET(request:Request){const code=new URL(request.url).searchParams.get("codigo")||"9000100";const n=Number(code.slice(-2))||1;return NextResponse.json({coordinate:{codigoCliente:code,latitude:10.96+(n%10)*.003,longitude:-74.82+(n%10)*.004}})}
