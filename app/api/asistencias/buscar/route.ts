import { NextResponse } from "next/server";export async function GET(request:Request){const dt=new URL(request.url).searchParams.get("dt")||"1760";return NextResponse.json({records:[{id:`a-${dt}`,dt,contratista:"Logísticos",cedulaResponsable:"10000101",nombreResponsable:"Carlos Mendoza",createdAt:new Date().toISOString()}]})}
